name: Awscli Installation
on: workflow_dispatch
jobs:
  awscli:
    runs-on: ubuntu-latest
    steps:
      - name: awscli-installation-command
        run: sudo apt install vim
      - name: check-version
        run: vim --version
  
